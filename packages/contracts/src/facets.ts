/** Facet counts as returned alongside results, so the client can render the panel. */
export {
  FacetsSchema,
  PRICE_BUCKET_WIDTH,
  type FacetOption,
  type Facets,
  type PriceBucket,
} from '@yaya/domain/facets.ts';
